import { WrapThemeColorPipe } from './wrap-theme-color.pipe';

describe('WrapThemeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new WrapThemeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
