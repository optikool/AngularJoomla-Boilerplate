import { EllipsisFormatterPipe } from './ellipsis-formatter.pipe';

describe('EllipsisFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new EllipsisFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
