import { TagFormatterPipe } from './tag-formatter.pipe';

describe('TagFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new TagFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
