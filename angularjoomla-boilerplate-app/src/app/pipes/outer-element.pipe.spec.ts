import { OuterElementPipe } from './outer-element.pipe';

describe('OuterElementPipe', () => {
  it('create an instance', () => {
    // const pipe = new OuterElementPipe();
    // expect(pipe).toBeTruthy();
  });
});
