import { SafeHtmlPipe } from './safe-html.pipe';

describe('SafeHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = true; // new SafeHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
